pipeline {
  agent {
    ecs {
      inheritFrom 'plugin-updates'
    }
  }

  stages {
    stage('Test') {
      steps {
        checkout scm
        sh 'ls -la /usr/bin'
        sh 'plugin-updates'
        sh 'git status'
      }
    }
  }
}
